// When createUserWithEmailandPassword and signInWithEmailandPassword called without errors.

onAuthStateChanged(auth, (user) => {
  if (user) {
    //User is logged in
    const name = user.displayName;
    const email = user.email;
    const uid = user.uid;
  } else {
    // User is logged out
  }
});
